/**
 * Placeholder amounts (Gehan said "e.g. Rs. 100-300", not final) — kept as
 * plain constants rather than a migration/admin setting so they're cheap to
 * tune later. Flag these three numbers to Gehan explicitly once this ships;
 * revisit as an Admin-configurable setting only if he actually asks for one.
 */
export const TEACHER_CONNECTION_FEE = 150;
export const CLASS_JOIN_FEE = 150;
export const INSTITUTE_UNLOCK_FEE = 150;
