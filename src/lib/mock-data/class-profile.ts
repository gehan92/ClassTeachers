/**
 * Placeholder data for Phase 1 (no Supabase project connected yet) — see
 * src/types/class-profile.ts for the shape. classProfiles/getClassProfile
 * used to live here; the public /class/[id] page is now wired to real
 * Supabase data instead (see src/app/[locale]/(public)/class/[id]/page.tsx).
 */
export {};
