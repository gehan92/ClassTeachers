/**
 * Placeholder data for Phase 1 (no Supabase project connected yet) — see
 * src/types/dashboard-admin.ts for the shapes. pendingApprovals,
 * platformUsers, siteAds, and flaggedReviews used to live here; all four are
 * now wired to real Supabase data instead (teacher_profiles/class_profiles,
 * Supabase Auth, advertisements, and reviews respectively — see
 * src/app/[locale]/admin/page.tsx).
 */
export {};
